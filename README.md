# Flask-restaurant-menu
small project to experiment with Flask and SQLAlchemy ORM. The app interacts with a database to show multiple restaurants and their menus with complete CRUD operations and routing.
